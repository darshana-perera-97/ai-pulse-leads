/**
 * Backend API base URL — no trailing slash.
 *
 * Use a full URL so both `npm start` and `npm run build` can reach the API.
 * Default matches the backend server (see backend/server.js PORT, often 369).
 *
 * To use Create React App dev proxy instead (package.json "proxy"), set to ''.
 */
export const API_BASE_URL = 'http://69.197.187.24:369';
// export const API_BASE_URL = 'http://localhost:369';
